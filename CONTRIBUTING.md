# 기여 안내

버그 수정, 설치 환경 검증, 접근성 개선과 번역을 환영합니다. 큰 변경은 먼저 이슈에서 범위와 기대 동작을 설명해주세요.

```sh
npm ci
npm test
npm run build
```

변경을 stage 한 뒤 `npm run check:public`으로 공개 파일을 검사합니다. 개인별 금지어는 저장소 밖에서 `PUBLIC_DENY_TERMS` 환경 변수로 전달할 수 있습니다. 실제 대화 원문·세션 ID·작업 경로·스크린샷을 이슈나 테스트 fixture에 올리지 마세요. 예시 데이터는 가상의 프로젝트와 직원으로 만듭니다.

관측 어댑터는 실제로 보고된 모델만 사용해야 합니다. 부모 기록, 기본 모델 설정, 응답 종료, 도구 재개를 각각 자식 모델·전체 업무 완료·승인 완료로 추정하지 않습니다. 원래 AI 앱에 명령이나 승인 응답을 보내는 기능은 이 프로젝트의 범위가 아닙니다.

Pull request에는 문제와 변경된 동작, 재현 또는 검증 결과를 적어주세요. 관측 프로토콜 변경은 서버·프런트엔드·어댑터 간 호환성과 재시작 시 상태를 함께 확인합니다. 기여 코드는 저장소의 MIT 라이선스로 제공됩니다.

macOS 다운로드판은 `npm run desktop:package`로 별도의 `.app`과 ZIP을 만듭니다. 기존 `desktop:build`·`desktop:install`은 별도 서버를 쓰는 개발용 창입니다. 업데이트 설정과 서명, 공개 전 검증은 [릴리스 안내](docs/RELEASING.md)를 따릅니다. 빌드·서명 명령은 GitHub에 자동으로 게시하지 않습니다.
